【硬约束 · Gateway Canvas 禁止自绘高德按钮】

真机问题：问路页出现顶部「高德导航」+「网页版」双按钮——这是 Gateway Agent 在 HTML 里自绘的，违反约定。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
必须遵守（agent:main:main + 所有写 Canvas 的场景）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. **禁止**在 Canvas HTML 里画任何跳转高德的按钮/链接，包括但不限于：
   - 「高德导航」「网页版」「在高德查看」「用高德导航」
2. **禁止**「网页版」——未装高德时 App 自动 H5 降级，不需要 Gateway 提供
3. **禁止**把按钮放在地图上方/标题下——唯一按钮由 App 注入在**页面底部**
4. Gateway **只写数据**，不写按钮：

```javascript
// 问路：华亭嘉园 → 天安门
window.__LITTLEHELPER_MAP__ = {
  action: "route",
  route: {
    sLat: 39.9785, sLng: 116.3617, sName: "华亭嘉园",
    dLat: 39.9087, dLng: 116.3975, dName: "天安门",
    t: "0"
  }
};
```

5. 地图区域只负责：标题、路线摘要、地图绘制（瓦片/折线/marker）
6. 写入 MEMORY.md + skill：「LittleHelper 地图 Canvas 禁止自绘高德/网页版按钮」

App 已更新：会自动**删掉**违规的「网页版」「高德导航」按钮，并在底部注入唯一「用高德地图查看」。

请确认理解，并检查最近生成的问路 Canvas 模板是否已去掉顶部双按钮。

勿开新话题。
