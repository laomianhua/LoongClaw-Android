# 复制本文件为 local.properties，填入本机 Android SDK 路径。
# local.properties 不会被 Git 提交。
#
# ── 开源 / 普通用户 ──
# 安装 APK 后请在 App 内 ⚙️ 设置填写 Gateway；无需改本文件。
#
# ── 开发者编译 ──
# 仅需 sdk.dir；Gateway 配置保存在 App 内（EncryptedSharedPreferences），不通过本文件注入。

sdk.dir=C\:\\Users\\你的用户名\\AppData\\Local\\Android\\Sdk

# ── 可选：开发者脚本联调（勿提交真实值）──
# 见 local.properties.lan.tmp.example；该临时文件已在 .gitignore 中排除。
