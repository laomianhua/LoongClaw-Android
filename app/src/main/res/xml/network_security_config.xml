<?xml version="1.0" encoding="utf-8"?>
<!--
  OpenClaw Gateway / Canvas 在 LAN 上使用 HTTP（非 HTTPS）。
  注意：一旦声明 networkSecurityConfig，Android 会忽略 Manifest 的 usesCleartextTraffic，
  必须在此处显式放行明文流量（WebView 白板加载依赖此项）。
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">100.112.96.116</domain>
        <domain includeSubdomains="false">192.168.1.55</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
